import cv2



"""
img1 = cv2.imread('images/ey_boss.jpg')
img2 = cv2.imread('images/pink_guy.jpg')
size = (160, 90)
img1 = cv2.resize(img1, size)
img2 = cv2.resize(img2, size)

phash1 = cv2.img_hash.pHash(img1)
phash2 = cv2.img_hash.pHash(img2)
print(phash1)
print(phash2)

phash1 = int.from_bytes(phash1.tobytes(), byteorder='big', signed=False)
phash2 = int.from_bytes(phash2.tobytes(), byteorder='big', signed=False)

print(phash1)
print(phash2)



print('difference: ' + str(phash1 - phash2))
"""